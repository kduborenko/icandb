task execute(type: JavaExec) {
    main = 'org.kd.icandb.App'
    classpath = sourceSets.main.runtimeClasspath
}

dependencies {
    compile project(':service')
    compile project(':in-memory-storage')

    runtime project(':logging')
}