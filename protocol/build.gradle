dependencies {
    compile project(':operations-api')
    compile project(':json-utils')

    compile group: 'org.springframework',
            name: 'spring-context',
            version: '4.0.5.RELEASE'
}